streamlit
python-docx
reportlab
pdfplumber
openpyxl
arabic-reshaper
python-bidi
anthropic
requests
pandas
pillow
matplotlib

# Google Gemini stack — only the top-level package is pinned; its
# sub-dependencies (grpcio, protobuf, google-api-core, etc.) are left
# unpinned on purpose so the installer can pick versions that match
# whatever Python version Streamlit Cloud is actually running.
google-generativeai==0.8.6
